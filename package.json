{
  "name": "codex-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local multi-model gateway and desktop manager for Codex.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "catalog": "node scripts/generate-catalog.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/server.js && node --check scripts/generate-catalog.js && node --test tests/*.test.js"
  }
}
